#include "common.h"

void func_800219C4(void) {
    register s32 var_t6 ASM_REG("$14");   /* UNRESOLVED C shape (pin): removing it changes the register colouring; the source shape that makes it unnecessary has not been found */

    var_t6 = 0x320000;
    do {
        var_t6 -= 1;
    } while (var_t6 != 0);
}
