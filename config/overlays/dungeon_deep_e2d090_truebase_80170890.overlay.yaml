# UNTRACKED prepare-only draft for the authenticated func_80E0D090 row.
# Install under config/overlays/ only after owner review and when the shared
# gate is released.  This draft is not a gate result and has not been run.
#
# The exact row is staged by the validation command in the pack REPORT.  The
# stage FUNC remains the synthetic split-row key so overlay_local_gate.py can
# find its extent; owner-decision-B rowbase handling then requires the source
# to define func_80170890 and mini-links it at the true base below.  The TU
# must never mention func_80E0D090.
#
# Minimal window and neighboring exclusions:
#   preceding data row: [0xE2D000,0xE2D090) (144 bytes), excluded
#   staged target row:  [0xE2D090,0xE2D2E8) (600 bytes)
#   following code row: [0xE2D2E8,0xE2D894) (1452 bytes), excluded
# The split-results loader sees only the middle row because this window is
# exactly the target range; no adjacent source is injected.
name: dungeon_deep_e2d090_truebase_80170890
sha1: 27f9fcc384a570a991e349b920881e7070c0e13c
options:
  basename: dungeon_deep_e2d090_truebase_80170890
  target_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
  symbol_addrs_path: config/overlays/dungeon_engine.symbols.txt
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  window:
    file_start: 0xE2D090
    file_end: 0xE2D2E8
    vram_start: 0x80170890
    true_vram_start: 0x80170890
    true_link_base: 0x80170890
    rowbase: sweep_0E2D000
    base_confidence: proven-rowbase
  split_results: overlays/dungeon/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/dungeon
      results: overlays/dungeon/overlay_first_pass_results.json
      source_dir: overlays/dungeon/first_pass_matched
      skip_rerun: True
