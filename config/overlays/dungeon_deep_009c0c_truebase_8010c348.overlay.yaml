# Standalone overlay-local gate for a statically base-proven DUNGEON
# per-floor overlay IMAGE.  vram_start is the image's TRUE link base, not
# the container's synthetic naming base -- hence the _truebase_ name marker
# (gate_carver._is_true_base_window keeps such windows out of the
# synthetic-convention vote).
# BASE PROOF: STATIC, three mutually independent EXACT-EQUALITY legs
# (2026-08-31 batch 3, work/wave_20260831_dungeon/rowbase_batch3_evidence.md).
# NOT in-function containment: this stub has no local `j`, no switch table
# and no leading inline jump table, which is why it sat base-less -- hence
# the generated 'n_pinned 0 of 1'.  The base rests on the sector mechanism:
# DUNGEON.BIN 0x7800-0xC000 is NOT one image and NOT the 0x8007D760 resident
# image -- it is a run of NINE 0x800-byte CD-SECTOR images, each streamed to
# its own base in the 0x8010xxxx slot.  A one-delta model of the run is
# refuted (the local-`j` containment windows of foff 0xA804 and foff 0xB828
# are disjoint under any single delta).
#   A  the FIRST WORD of this row's sector is a pointer, in true space, to
#      the stub inside it: word[0x9800] = 0x8010C348 = this window's
#      file_start, so K = 0x8010C348 - 0x9C0C = 0x8010273C.
#   C  independent of A: the stub's own lui/addiu materialises
#      0x8010C0F8 and stores it into the object it allocates at
#      +0x10 -- its own sector's LEADER at foff 0x99BC, same K.
#   B  independent of both: that leader's only 0x8010xxxx jal targets
#      0x8010C274, its own sector's MID at foff 0x9B38, same K.
# All three agree on all NINE sectors of the run, 0 disagreements.
# Evidence A is NOT claimed (a_pass false): the stub's two jal 0x8003CF18 /
# 0x8003A7C4 are not function ENTRIES in config/func_sizes.json but mid-body
# landings (+0x268 into func_8003CCB0, +0x8 into func_8003A7BC).  Both
# encodings occur 18x each in DUNGEON.BIN and 0x in MAIN/TOWN/OVMOVIE, all 36
# sites inside 0x7800-0xC000 -- census gaps local to this module, and
# base-independent within region 0x8 either way.
# EXTENT is the ROW, not the 0x800 sector: the sector extent overlaps existing
# SYNTHETIC-base dungeon_deep_t8b_* windows (two different claims about the
# same bytes, in two different 256 MB jal regions), the sector head holds no
# detected function start, and this extent matches the ledger record and the
# row exactly.  Ledger: config/overlays/dungeon.rowbase.jsonl row_9c0c_8010c348
# (base_confidence `solved`; promotion needs a byte-exact recompile here).
# Image img0395@0x9C0C of census run 0x7800-0xc000 (nine 0x800-byte CD-sector images);
# Evidence A confirm=0 contra=0, Evidence B b1=0 b2=0/0 violations=0.
# Generated by tools/dungeon_image_windows.py --apply; do NOT add to
# configure.py / build.ninja; run via tools/overlay_local_gate.py --clean.
name: dungeon_deep_009c0c_truebase_8010c348
sha1: 27f9fcc384a570a991e349b920881e7070c0e13c
options:
  basename: dungeon_deep_009c0c_truebase_8010c348
  target_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
  symbol_addrs_path: config/overlays/dungeon_engine.symbols.txt
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  window:
    file_start: 0x9C0C
    file_end: 0x9D14
    vram_start: 0x8010C348
    # Stated again as true_vram_start so every consumer that keys on that
    # field (tools/campaign_sentinels.py, tools/test_tool_contracts.py)
    # reads the same proven runtime base as vram_start.
    true_vram_start: 0x8010C348
    base_confidence: jump-implied
  split_results: overlays/dungeon/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/dungeon
      results: overlays/dungeon/overlay_first_pass_results.json
      source_dir: overlays/dungeon/first_pass_matched
      skip_rerun: True
