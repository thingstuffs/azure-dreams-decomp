# Tight standalone gate for func_81911BF4 at its authenticated true link base
# (fan E landing, 2026-08-27). The row IS the whole jtbl_1931BF4 rowbase region
# (foff [0x1931BF4, 0x1932074) exactly matches the region's own extent), which
# tools/rowbase.py promote flipped solved->proven this session using this row's
# own byte-exact recompile as the T1 proof. This window is the row extent
# exactly -- no other banked row shares it.
# Base: func_800253F4 (config/overlays/dungeon.rowbase.jsonl / row true_name).
name: dungeon_deep_1931bf4_truebase_800253f4
sha1: 27f9fcc384a570a991e349b920881e7070c0e13c
options:
  basename: dungeon_deep_1931bf4_truebase_800253f4
  target_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
  symbol_addrs_path: config/overlays/dungeon_engine.symbols.txt
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  window:
    file_start: 0x1931BF4
    file_end: 0x1932074
    vram_start: 0x800253F4
    base_confidence: proven-rowbase
  split_results: overlays/dungeon/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/dungeon
      results: overlays/dungeon/overlay_first_pass_results.json
      source_dir: overlays/dungeon/first_pass_matched
      skip_rerun: true
