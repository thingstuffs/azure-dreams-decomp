# Standalone overlay-local gate seed -- DEEP/BACK-HALF town subwindow (TF-1B gate_carver).
# Do NOT add to configure.py / build.ninja; run via tools/overlay_local_gate.py --clean.
#
# NEW window for 1 banked byte-verified candidate(s) that lacked an economically bounded verification window
# (result==NOMATCH in the split JSON,
# closed by mutation/knob/model search), so overlay_reconcile_lane can freeze + land them:
#   func_8032DC20
#
# BASE NOTE -- base_confidence: synthetic-self-consistent (copied from config/overlays/town_deep_t8b_00e8a4.overlay.yaml, NOT upgraded).
# vram_start is the func_ synthetic naming base only (chain-G trap b4ae80e) -- no proven
# load base here, so no true_vram_start. Provenance: tight verification shadow for economically broad gate town_deep_t8_02ab (config/overlays/town_deep_t8_02ab.overlay.yaml, 0x2ABFE8..0x2AEC48); base synthetic-self-consistent from config/overlays/town_deep_t8b_00e8a4.overlay.yaml; rowbase-proven true base 0x80018420 (delta 0x7FD6A800, docs/rowbase_lane_phase2.md)
# Expected first state: targets are result==NOMATCH -> all-raw window == container bytes.
name: town_deep_t8b_2adc20
sha1: b946a57aa5fc8cec29db3ecabad6e6b76df418b7
options:
  basename: town_deep_t8b_2adc20
  target_path: work/s3_splat/extract/TOWN_TOWN.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/TOWN_TOWN.BIN
  window:
    file_start: 0x2ADC20
    file_end: 0x2ADCB8
    vram_start: 0x80018420          # = 0x2ADC20 + 0x7FD6A800 (rowbase-proven true base (config/overlays/<c>.rowbase.jsonl))
    base_confidence: synthetic-self-consistent   # copied; see BASE NOTE
  split_results: overlays/town/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/town
      results: overlays/town/overlay_first_pass_results.json
      source_dir: overlays/town/first_pass_matched
      skip_rerun: True
