# Standalone overlay-local gate seed -- DEEP/BACK-HALF dungeon subwindow (TF-1B gate_carver pattern).
# Do NOT add to configure.py / build.ninja; run via tools/overlay_local_gate.py --clean.
#
# NEW window for 1 sol-wave byte-verified rowbase-rename candidate that lacked an economically
# bounded verification window, so it can be gated and landed at its PROVEN TRUE BASE (owner
# decision B requires the TU to define only the true name, and the gate links this window
# there, not at the synthetic naming base):
#   func_80D3C280 -> func_80171A80
#
# BASE NOTE -- base_confidence: synthetic-self-consistent (copied from config/overlays/dungeon_deep_t8b_0b14c4.overlay.yaml, NOT upgraded).
# vram_start is the func_ synthetic naming base only (chain-G trap b4ae80e) -- no proven
# load base here, so no true_vram_start. Provenance: tight verification shadow for economically broad gate dungeon_engine (config/overlays/dungeon_engine.overlay.yaml, 0xA0000..0x100000); base synthetic-self-consistent from config/overlays/dungeon_deep_t8b_0b14c4.overlay.yaml; rowbase-proven true base 0x80171A80 (delta 0x7F415800, docs/rowbase_lane_phase2.md, region sweep_0D5B284).
# Expected first state: targets are result==NOMATCH/M2C_FAIL -> all-raw window == container bytes.
name: dungeon_deep_t8b_d5c280
sha1: 27f9fcc384a570a991e349b920881e7070c0e13c
options:
  basename: dungeon_deep_t8b_d5c280
  target_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
  symbol_addrs_path: config/overlays/dungeon_engine.symbols.txt
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  window:
    file_start: 0xD5C280
    file_end: 0xd5c944
    vram_start: 0x80171A80          # = 0xD5C280 + 0x7F415800 (rowbase-proven true base (config/overlays/<c>.rowbase.jsonl))
    base_confidence: synthetic-self-consistent   # copied; see BASE NOTE
  split_results: overlays/dungeon/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/dungeon
      results: overlays/dungeon/overlay_first_pass_results.json
      source_dir: overlays/dungeon/first_pass_matched
      skip_rerun: True
