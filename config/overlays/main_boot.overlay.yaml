# Standalone overlay-local build seed.
# Do NOT add to configure.py / build.ninja; use tools/overlay_local_gate.py for
# the isolated MAIN boot/menu overlay gate.
#
# MAIN boot/menu candidate overlay, carved from MAIN/MAIN.BIN.
# DERIVED load map: vram = file_offset + 0x7FDB0000
#   file 0x260000 -> vram 0x80010000, matching the coherent internal jal
#   cluster documented in docs/overlay_inventory.md section 3.
name: main_boot
sha1: 69f63236b7ab2260e2e9aa2048ce7aa540bdbe6d
options:
  basename: main_boot
  target_path: work/s3_splat/extract/MAIN_MAIN.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/MAIN_MAIN.BIN
  window:
    file_start: 0x25D000
    file_end: 0x272000
    vram_start: 0x8000D000
    base_confidence: derived
  split_results: overlays/main/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/main
      results: overlays/main/overlay_first_pass_results.json
      source_dir: overlays/main/first_pass_matched
      skip_rerun: true
segments:
  - [0x0, bin]
  - type: code
    start: 0x25D000
    vram: 0x8000D000
    subsegments:
      - [0x25D000, asm]
  - [0x272000, bin]
  - [0x275800]
