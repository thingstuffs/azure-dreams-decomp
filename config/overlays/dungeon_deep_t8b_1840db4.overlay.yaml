# Standalone overlay-local gate seed -- DEEP/BACK-HALF dungeon subwindow (TF-1B gate_carver pattern).
# Do NOT add to configure.py / build.ninja; run via tools/overlay_local_gate.py --clean.
#
# NEW tight window for 1 sol-wave byte-verified candidate that lacked an economically bounded
# verification window (its broad parent window, dungeon_deep_t8_1830, no longer byte-verifies as a
# whole -- see BASE NOTE), so it can be gated and landed independently:
#   func_81820DB4
#
# BASE NOTE -- base_confidence: synthetic-self-consistent (copied from config/overlays/dungeon_deep_t8_1830.overlay.yaml, NOT upgraded).
# vram_start is the func_ synthetic naming base only (chain-G trap b4ae80e) -- no proven
# load base here, so no true_vram_start. Provenance: tight verification shadow for broad gate
# dungeon_deep_t8_1830 (config/overlays/dungeon_deep_t8_1830.overlay.yaml, 0x1830080..0x186AAA0),
# following the same pattern as dungeon_deep_t8b_18324f0.overlay.yaml (func_818124F0, landed
# 2026-08-14). The broad window's --clean rebuild currently fails at window+0x25B3 / file
# 0x1832633 (inside func_818124F0's rowbase-linked reuse at the WRONG synthetic base -- its own
# proven true base is 0x800274F0, see dungeon_deep_t8b_18324f0.overlay.yaml), a PRE-EXISTING
# defect unrelated to func_81820DB4 (reproduces identically with func_81820DB4 unstaged); this
# tight window is far enough away (0x1840DB4 vs 0x18324F0, ~0xE8C4 apart) to avoid that neighbor
# entirely and gate func_81820DB4 in isolation instead of waiting on the broad window's repair.
# Expected first state: target is result==NOMATCH -> all-raw window == container bytes.
name: dungeon_deep_t8b_1840db4
sha1: 27f9fcc384a570a991e349b920881e7070c0e13c
options:
  basename: dungeon_deep_t8b_1840db4
  target_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
  symbol_addrs_path: config/overlays/dungeon_engine.symbols.txt
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  window:
    file_start: 0x1840DB4
    file_end: 0x184121C
    vram_start: 0x81820DB4          # = 0x1840DB4 + 0x7FFE0000 (synthetic sweep base)
    base_confidence: synthetic-self-consistent   # copied; see BASE NOTE
  split_results: overlays/dungeon/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/dungeon
      results: overlays/dungeon/overlay_first_pass_results.json
      source_dir: overlays/dungeon/first_pass_matched
      skip_rerun: True
