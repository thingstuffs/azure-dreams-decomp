# Standalone overlay-local gate seed -- DEEP/BACK-HALF main subwindow (TF-1B gate_carver).
# Do NOT add to configure.py / build.ninja; run via tools/overlay_local_gate.py --clean.
#
# NEW window for 1 banked byte-verified candidate(s) that lacked an economically bounded verification window
# (result==NOMATCH in the split JSON,
# closed by mutation/knob/model search), so overlay_reconcile_lane can freeze + land them:
#   func_8001293C
#
# BASE NOTE -- base_confidence: derived (copied from config/overlays/main_boot.overlay.yaml, NOT upgraded).
# vram_start is the func_ synthetic naming base only (chain-G trap b4ae80e) -- no proven
# load base here, so no true_vram_start. Provenance: work-local bootstrap proposal: exact one-function row under main_boot; fields copied from covering gate; normal derive_convention still refuses until a main_deep_t8b reference is reviewed
# Expected first state: targets are result==NOMATCH -> all-raw window == container bytes.
name: main_deep_t8b_26293c
sha1: 69f63236b7ab2260e2e9aa2048ce7aa540bdbe6d
options:
  basename: main_deep_t8b_26293c
  target_path: work/s3_splat/extract/MAIN_MAIN.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/MAIN_MAIN.BIN
  window:
    file_start: 0x26293C
    file_end: 0x262B38
    vram_start: 0x8001293C          # = 0x26293C + 0x7FDB0000 (synthetic sweep base)
    base_confidence: derived   # copied; see BASE NOTE
  split_results: overlays/main/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/main
      results: overlays/main/overlay_first_pass_results.json
      source_dir: overlays/main/first_pass_matched
      skip_rerun: True
