# Standalone overlay-local gate for a statically base-proven DUNGEON
# per-floor overlay IMAGE.  vram_start is the image's TRUE link base, not
# the container's synthetic naming base -- hence the _truebase_ name marker
# (gate_carver._is_true_base_window keeps such windows out of the
# synthetic-convention vote).
# BASE PROOF: STATIC, IMAGE MEMBERSHIP (2026-08-31 batch 3,
# work/wave_20260831_dungeon/rowbase_batch3_evidence.md).
# NOT in-function containment: this row is POSITION-INDEPENDENT -- byte-identical
# to its copies at +0x6000/+0xC000/+0x12000 (0 of 162 words differ at each) and
# carrying no local `j` -- so its own bytes cannot state a base.  Hence the
# generated 'n_pinned 0 of 1'.
#   foff 0x1000000-0x10F5000 is a 7-SLOT ROUND-ROBIN floor cache: images sit on a
#   strict 0x6000 file grid and their load addresses cycle 0x80170800 ->
#   0x8016A800 -> 0x80164800 -> 0x8015E800 -> 0x80158800 -> 0x80152800 ->
#   0x8014C800 and WRAP.  21 of 21 existing sweep_*/img_* records in the band obey
#   it with zero exceptions.
#   D  this row's 0x6000 cell [0x104F000,0x1055000) already contains TWO
#      base_confidence PROVEN ledger records stating exactly this K 0x7F121800:
#      img_104f674_80170e74 [0x104F674,0x104F83C) and sweep_104F83C
#      [0x104F83C,0x1054824).  The row ends at 0x104F5D0, 0xA4 before the first.
#      This is membership, not ladder interpolation.
#   E  the row's three byte-identical siblings sit at the same +0x348 offset
#      inside images 0x1055000 / 0x105B000 / 0x1061000, each of which likewise
#      carries its first recorded member at +0x674.  Control: the copies at
#      -0x6000/-0xC000/-0x12000 differ in 160 of 162 words.
# REFUTES the prior inventory value link_base 0x7f0fd800 / name func_8014CB48,
# which assumed the ladder kept DESCENDING past the bottom slot instead of
# wrapping: it would put image 0x104F000 at 0x8014C800, the same slot as image
# 0x1049000, contradicting both PROVEN records inside the row's own image.
# The 0x1E000 gap the prior pass read as 'the rungs are not adjacent' IS the wrap
# (file +0x6000 while RAM +0x24000).  True name: func_80170B48.
# EXTENT is the ROW, not the 0x6000 cell: the cell overlaps 13 existing gate
# windows (one of them dungeon_deep_104f674_truebase_80170e74) and would re-claim
# bytes two PROVEN records already own.  Ledger: config/overlays/
# dungeon.rowbase.jsonl row_104f348_80170b48 (base_confidence `solved`).
# Image img0398@0x104F348 of census run 0x1000000-0x10f5000 (7-slot round-robin floor cache, 0x6000 grid);
# Evidence A confirm=1 contra=0, Evidence B b1=0 b2=0/0 violations=0.
# Generated by tools/dungeon_image_windows.py --apply; do NOT add to
# configure.py / build.ninja; run via tools/overlay_local_gate.py --clean.
name: dungeon_deep_104f348_truebase_80170b48
sha1: 27f9fcc384a570a991e349b920881e7070c0e13c
options:
  basename: dungeon_deep_104f348_truebase_80170b48
  target_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
  symbol_addrs_path: config/overlays/dungeon_engine.symbols.txt
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  window:
    file_start: 0x104F348
    file_end: 0x104F5D0
    vram_start: 0x80170B48
    # Stated again as true_vram_start so every consumer that keys on that
    # field (tools/campaign_sentinels.py, tools/test_tool_contracts.py)
    # reads the same proven runtime base as vram_start.
    true_vram_start: 0x80170B48
    base_confidence: jump-implied
  split_results: overlays/dungeon/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/dungeon
      results: overlays/dungeon/overlay_first_pass_results.json
      source_dir: overlays/dungeon/first_pass_matched
      skip_rerun: True
