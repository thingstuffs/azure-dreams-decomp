# Standalone overlay-local gate seed -- DEEP/BACK-HALF dungeon subwindow (TF-1B gate_carver pattern).
# Do NOT add to configure.py / build.ninja; run via tools/overlay_local_gate.py --clean.
#
# NEW window for 1 post-land twin-cascade byte-verified candidate (deterministic twin-port
# from func_80DA7000, sim 0.993) that lacked an economically bounded verification window:
#   func_80D95000
#
# BASE NOTE -- base_confidence: synthetic-self-consistent (copied from config/overlays/dungeon_deep_t8b_0b14c4.overlay.yaml, NOT upgraded).
# vram_start is the func_ synthetic naming base only (chain-G trap b4ae80e) -- confirmed
# NOT a proven rowbase region (tools/rowbase_rename_reverify.py refused a rename: "not in a
# PROVEN rowbase region"), so the TU keeps its synthetic name. Provenance: tight verification
# shadow for economically broad gate dungeon_engine (config/overlays/dungeon_engine.overlay.yaml, 0xA0000..0x100000).
# Expected first state: targets are result==NOMATCH/CFAIL -> all-raw window == container bytes.
name: dungeon_deep_t8b_d95000
sha1: 27f9fcc384a570a991e349b920881e7070c0e13c
options:
  basename: dungeon_deep_t8b_d95000
  target_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  base_path: ..
  platform: psx
  compiler: GCC
  subalign: 4
  find_file_boundaries: False
  gp_value: 0x80080994
  symbol_addrs_path: config/overlays/dungeon_engine.symbols.txt
standalone_build:
  work_dir: work/s3_splat
  extract:
    extractor: work/overlay_recon/extract_bins.py
    disc_path: "bin/Azure Dreams.bin"
    out_dir: work/s3_splat/extract
    container_path: work/s3_splat/extract/DUNGEON_DUNGEON.BIN
  window:
    file_start: 0xDB5000
    file_end: 0xdb523c
    vram_start: 0x80D95000          # = 0xDB5000 + 0x7FFE0000 (synthetic sweep base)
    base_confidence: synthetic-self-consistent   # copied; see BASE NOTE
  split_results: overlays/dungeon/overlay_first_pass_results.json
  matched_sources:
    - name: overlays/dungeon
      results: overlays/dungeon/overlay_first_pass_results.json
      source_dir: overlays/dungeon/first_pass_matched
      skip_rerun: True
