# Round46: one mechanical transfer and a natural shared address

The direct-helper sibling `8132E5EC` removes **one pin**, leaving **6,049 in
1,275 rows**: 54 removed since the reopened baseline. Callee/caller are strict
67/67 and 319/319; both covering windows and SLUS MATCH; row database and research
ledger pass. See [the durable receipt](evidence/pin_research_round46_20260917.json).
Snapshot140
now produces its formerly missing partial address through ordinary indexed C;
that function remains nonexact and no snapshot source has been promoted.

## Direct table argument

The finite audit covers 2,743 DUNGEON source files below 4 MiB: the Position144
shape is unique. Of 111 files with the allocator/render-helper pair, fifteen are
in the same effect-image cluster and ten already have zero pins. One pinned
member has an immediate transfer: its port arm already uses `D_800DE870`, while
the matching arm manufactures that address with a page local and keep.

Using the direct table argument and actual signed-short helper index removes
the one pin and arm split. It is strict 67/67 at stock CDK. Old source matches both
plain 2.7.2 andCDK, so the normal automatic cell-transition controls apply. The
candidate misses at plain 2.7.2. Delay-slot stores support the CDK/2.8 family,
without uniquely identifying a release.

The sole same-image caller's declaration is corrected from an unknown scalar
return/short parameters to `void`/s32 parameters, matching the callee. Its
explicit coordinate conversions remain; both calls discard the result. The
caller is strict 319/319 at its unchanged recipe. Port assembly equality permits
the callee arm collapse under the existing rule. See
[independent review](evidence/pin_research_direct_helper_review_20260917.md).

## Snapshot140: changed benefit, rather than a forced page

Three grounded simplifications reduce the second loop's compiler instruction
count 36->34->33->32. The address HIGH still has lifetime 1/savings 1 and is rejected.
Static inspection of the exact unstripped CDK executable establishes threshold 30:
29 nonfixed registers plus one, multiplied by one for a call loop. The Sony
register table differs from retained FSF 2.7.2, whose count would mispredict 29.
The ordinary rule is unchanged across the locally retained compiler sources.

The successful causal change is an actual buffer subscript at the copy:
`((Blob140 *)D_800E3E48)[slot_index]`. This gives the symbol two real uses inside
the loop. Its HIGH now has lifetime 8/savings 3 and is hoisted despite the loop
growing to 39 instructions; the lookup's HIGH matches it. The 56-byte frame and
saved roles return, first 80 words match, and the full zero-pin source scores
182/181, total 27. No fake use, new page binding or compiler patch is involved.

Removing dead inherited cursor initialization/update also removes an extra
preheader move, yielding 181/181, total 35. It swaps input/HIGH saved roles: global
allocation now considers HIGH before input. The byte-index and four-byte-copy
cursors also undergo explicit induction-variable rebasing. The new questions
are allocator order and real cursor source relationships. Ordinary C's ability
to retain the partial symbolic address is no longer an open question here.
The cleaner source must guide further work; dead initializations must not be
preserved merely to manipulate allocation.

A separate bounded partial-repair lane replaces only the first copy region
while retaining unrelated existing pins. The best has17 pins and181/181 words,
with13 register substitutions in the first loop and all words 80–180 exact.
Retaining the original record binding makes the aggregate mutate that register
and loses the retail copy roles. The original entry-cursor keep changes the
role permutation without fixing it. A typed larger record/prefix is the next
bounded source hypothesis. No new hard binding is allowed to replace
removed keeps. Failed controls remain bounded negatives, not impossibility.
