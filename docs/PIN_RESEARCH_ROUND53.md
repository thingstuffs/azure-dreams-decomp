# Round 53: whole-function delegation removes four pins

**Four pins removed: 6,024 remain in 1,270 functions; 79 removed from 6,103.**
The complete row, its covering town_scene window, host syntax, SLUS and row
database pass. No recipe, compiler, assembler or shared helper interface changes.
The [durable receipt](evidence/pin_research_round53_20260917.json) retains the
sources, full comparisons, bounded negatives, runtime traces and reviews.

## A bounded Sol reconstruction reaches zero

Sol received the complete `town_map_set` function (`town/func_8008DEF4`), retail
instructions, fixed `2.8.0-G0` recipe and four-hypothesis budget. It reconstructed
allocation of eleven object/resource pairs, reverse rollback of completed pairs,
and initialization of their linked data. One real index is reused across the
allocation and initialization phases and initialized before the object cursor.
This supplies retail's `s2` index and `s1` cursor roles without constraints.

All four pins disappear together: two hard register bindings, including zero,
and two keeps. The final source matches all 104 words / 416 bytes with strict
relocations. The old source is exact at the same recipe. The four hypotheses
score 33, 49, 29 and zero aligned edits; this is complete-function recovery,
not a claim that four independent pin deletions work. The two-argument entry
and real helper calls are preserved. Failure after object allocation retains
retail's existing behavior: only completed pairs enter rollback.

Root reproduced both exact endpoints and the strict comparison. Independent
[source/caller review](evidence/pin_research_town_map_review_20260917.md) and full
publication gates passed. The frozen site map labels row-space addresses; the
retained disassembly and strict comparison use proven runtime `8008B654`. The old row-recorded source hash differs from live
source; both were frozen, and publication checks the actual live preimage.

A single 3.29 MB scan of town C for the three named allocation/cleanup helpers
finds only this row. This is a narrow lexical index, excludes aliases, and does
not establish that the source-lifetime mechanism has no other applications.
There are **zero independently verified transfers of this new repair yet**.

## Exact cache evidence, bounded source negatives

For transition `8195F0BC`, the exact CDK runtime trace now follows cached zero:
UID350 records `s3=0` in SI mode; the cache survives a conditional branch and
is available at UID362's column initialization. Loop label365 clears it before
UID368. This explains why moving the reset across the loop entry avoided the
copy in earlier probes, while changing allocator roles elsewhere.

A genuine `u16` row counter changes the cached value to HI mode. The runtime
comparison rejects it for the SI column initialization, and the literal zero
survives. However, the complete function gains masks and five instructions:
229/224 words, 12 substitutions and five indels. Removing its row-register
binding worsens the result to 237/224 words, 43 substitutions and 21 indels.
Neither candidate lands. The best remains five pins, 224 words, one substitution.

Both traced assembly streams equal ordinary compiles; independent review
confirms [the measured scope](evidence/pin_research_zero_cache_review_20260917.md).
Instrumentation does not write compiler RTL/data; debugger breakpoints can
transiently patch process text. Addresses and mode numbers are specific to the
recorded compiler hash. The exact binary gates this reload-CSE call on optimization,
not `flag_expensive_optimizations`; no ineffective flag probe was run.

## Independent zero-binding controls

The pre-landing inventory contains 24 live zero-register sites in 22 files:
four loop initializations, fourteen returns, three immediate constructions and
three other uses. The successful town-map reconstruction removes one site.
The remaining inventory is not a forecast of 23 mechanically removable pins.

Luna tested literal-zero replacements in two independent functions, verifying
both exact baselines. Each candidate removes one pin but misses one substitution:
`800AE78C` copies zero from `s4` into `s7`; `800B89C0` returns it from `s0`.
The latter's original negative prediction missed a real `stored=0` producer
and is explicitly retracted. These are two observed reuse cases, not repairs.

The pass boundaries differ: the first copy appears in `greg`, while the second
is still literal zero there and changes by `dbr`. The exact latter pass owner
has not been traced. Do not group both under the proven CDK reload-CSE routine
merely because their final instructions look similar.

## Gemini and the next allocation of work

The installed `agy` CLI and model listing work. The old launcher/harvester in
`~/agy_lane/` and `tools/lanes/launch_lane.sh` provide Gemini lanes. This round's
read-only full-function rubber-duck request used `gemini-3.8-flash-high`, the
complete transition assembly/source, actual cache traces and previous failures.
It hit the three-minute print timeout after 187.7 seconds with empty partial
output and exit zero. This is an incomplete run, not a negative research result.

Use fresh bounded function packs with full retail, actual helper contracts,
fixed recipes, saved candidates and checkpoints. Sol's four-pin result adds
concrete evidence that root need not do every reconstruction. Reserve root
work for unresolved compiler questions and independent publication review.

The remaining 6,024 sites are not 6,024 established distinct patterns. Earlier
base-address and aggregate-copy transfers are proven; this round adds one
whole-function repair and better diagnostic discrimination. Before mechanizing
shared-counter recovery, identify a second genuine multi-phase traversal and
predict its register/lifetime effect. For the transition, require a new causal
fact before another source menu. No function is classified impossible.
