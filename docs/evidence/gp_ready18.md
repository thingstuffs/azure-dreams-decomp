# Combined 18-function private rehearsal

## Current rebase after command-slot activation

The [fresh rebase receipt](gp_ready18_rebased_81554/receipt.json) preserves the
activated 81554/58 owner and repeats all **891 words across 18 functions** with
fresh generic, genuine and linked-object equality, zero masks and an exact full
retail image. The [primary review](gp_ready18_rebased_81554/primary_review.json)
independently checks the complete raw function sets in all twelve physical
streams. Production remains unchanged by this private experiment.

The baseline is now **23 modules / 869 physical / 884 logical rows**. The private
trial has 30 modules, 864 physical inputs, 56 exact owned data declarations and
191 functions in the connected group. All 851 outside active objects are
unchanged. The existing command-slot owner uses stock assembly throughout;
only the same twelve prepared inputs use the private correction. Eight proposed
recipe changes remain, and the current baseline agrees with the current logical
ledger without a recipe exception.

Production has **189 dependencies and 21 GP rows**. Twelve GP rows are prepared
in this combined trial; nine remain: `code`, `code2`, 3E34C, 3E4FC, 3E758,
3F2A4, 3F368, 3F5AC and 3F624. The 18-function scope still comprises seventeen
whole rows and one collector part, not a complete genuine proof of `code`.
The global assembler switch and pass retirement remain pending their full gates
and existing owner signoff.

Reproduce using [the current script metadata](gp_ready18_rebased_81554/reproducer.json).
The following original checkpoint and its receipts remain as historical evidence.

## Original 22-module checkpoint

All **891 words across 18 functions** match fresh generic assembly, genuine
ASPSX 2.79 and retail with zero masks, and coexist in one full retail-exact
private image. The [receipt](gp_ready18/receipt.json) preserves source hashes,
recipes, actual linked-object comparisons, data placement and production
before/after snapshots. Production remains unchanged.

This combines the [rebased 14-row preparation](gp_ready14.md) with the
[81550 declaration and data repair](gp_81550_partition.md). The latter contributes
three whole rows plus one explicit function part from `code`. There are 17
prepared whole rows and one collector function; this is not an 18-row proof of
complete genuine equality for the `code` collector.

| Property | Production baseline | Private trial |
|---|---:|---:|
| Ownership modules |22|29|
| Active physical inputs |872|867|
| Original logical rows |884|884|
| Generic-corrected physical inputs |0|12|

The trial preserves all 22 active owners and adds seven units, replacing twelve
whole-member inputs. It retains the existing E0/B98/81540 function partitions and
adds only the 81550 setter. All 187 connected functions occur exactly once. The
only eight logical recipe differences are the earlier seven plain-to-CDK moves
and removal of DCEC's two scheduling flags at its reviewed script declaration.
No new flags, pins or assembler rules are introduced.

All 54 owned data declarations have exact section bytes, object offsets, asset
slices and non-absolute linked addresses. All 854 unchanged active outputs match
production byte for byte. The full image equals retail SHA-256
`ae19d8d4bdd2db9a933b1e44d70dd61be670781e441d7cb37da85250a7aed31f`.
The [primary review](gp_ready18/primary_review.json) also checks complete physical
function sets independently in every linked, fresh generic and genuine stream.
No extra or missing function can hide behind the selected comparison scope.

The current ledger has 25 GP rows. Twelve are prepared here; thirteen remain:
`code`, `code2`, seven CD-state rows (3E34C, 3E4FC, 3E758, 3F2A4, 3F368, 3F5AC, 3F624)
and four 81554/58 rows (4DCA8, 4DD2C, 4DDE4, 4E130). The collector still contains
unrelated GP users. Its moved setter is exact, not its entire genuine output.

Only the 12 prepared physical inputs use the private assembler. This is a
diagnostic coexistence proof, not an acceptable production whitelist or global
transition gate. Production still has 193 dependency records and 25 GP rows;
none are removed from a private rehearsal. Complete the remaining ownership and
source repairs, apply the proposed assembler correction to all inputs, refresh
the dependency census and required gates, then seek the existing final owner
signoff on that concrete patch. Pass retirement remains a separate consumer queue.

Reproduce with `python3 work/native_lane/gp_ready18_combined/probe.py`.
[Reproducer metadata](gp_ready18/reproducer.json) identifies the measured script.
