# Guarded generic assembler: full overlay gate

The exact private guarded candidate passed the complete forced overlay-window gate: **2,175/2,175 current windows MATCH**, exit code zero, with no serial retries. The [receipt](receipt.json) binds the candidate CLI SHA-256 `d3e6ec7d120abe00f963c82258dfdeb0e228774adce646177525f1dbed925caf` and package SHA-256 `28e45bbcf00fc90dc0645250f233b73f96ad11d06a2f8230ce533fc2fafbbfbe` before and after. Both assembler override variables pointed at that exact CLI. The [journal](journal.jsonl) has one MATCH record per window (SHA-256 `a1b70d3ee87c704e9007439cdca4dd72778712a088ec33ec440e3fcb6aa9abc5`); the [gate log](gate.log) records the forced `--all --workers 4` run and final current-input check.

The gate used unique `build_ovl_guarded_20260924_sol` from `EXP=guarded_20260924_sol SRCROOT="$PWD/src" bash tools/build/mk_ovl_root.sh`. `GATE_BUILD_ROOT` named that root and `GATE_JOURNAL` named this private journal; production ledger was untouched. The tracked `src`, `config`, `tools`, `include`, and `ledger/splits` input snapshot was byte-identical before and after (9,749 files; aggregate SHA-256 `f1dd11c3af72327fadb55359e4f23632584fe2028f9a1bcfb5dc2bf9dc56fb47`). Candidate hashes were unchanged. [run.py](run.py) contains the exact command and hash procedure; [setup.log](setup.log) records isolated-root preparation.

The package import check resolved `work/native_lane/selfinc_local_guard/candidate/maspsx/__init__.py` ([log](unit_import.log)). The focused `_fold_selfinc_la` unit tests passed **6/6** ([log](unit_selfinc.log)). The full existing suite ran **417 tests with 13 failures** under the guarded package ([log](unit.log)); the generic-before package has the **same 13 failure names and identical failure output**, aside from elapsed time ([log](unit_before.log)). These failures reflect existing generic-versus-production semantics, especially `.extern` handling. This suite is therefore a before/after no-new-failure check, not a passing generic unit-suite gate. The 18-case genuine-ASPSX micro and 70-case replay remain the direct positive validation of the new local-data guard.

This overlay result adds non-SLUS whole-window coverage to the separate SLUS owner and all-object trials. It does not refresh the genuine-ASPSX dependence census or justify retiring `_fold_selfinc_la` while its twelve direct consumers still require it.
