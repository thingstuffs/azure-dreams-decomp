# Final private full-ownership proof

**PASS.** A fresh detached fixture uses the frozen 57-file transition payload, ready29 stage, guarded generic assembler, four comment/unused-macro cleanups, and four canonical-source organization files. The final payload's four organization files match the reviewed overlay exactly. No production or frozen ready29/census/live fixture was written.

The normal private `tools/configure.py` generated `build.ninja` byte-identical to the pinned full ledger and ready29 graph (SHA-256 `968784a7addd9c20cca57e34f3f28d73a101dcc88c91e5046adbe55d0a7cdc44`). Its 860 physical edges project to all 884 logical SLUS rows; the 6,767-row registry preserves the complete row-ID set, and the 33 modules declare 70 data records. Normal Ninja linked a byte-for-byte retail image (SHA-256 `ae19d8d4bdd2db9a933b1e44d70dd61be670781e441d7cb37da85250a7aed31f`). All 860 object hashes equal ready29, including the two reorganized owners. Exact allocated sections, relocation records, and named symbols were also checked for those two owner objects.

The unchanged candidate prover CLI ran through a private wrapper that gives genuine ASPSX a unique short `/tmp` scratch path; it invokes the real `A.process_row` and preserves the explicit `--compiler-model-row slus/konami_runtime_w_80033D54` argument. The schema-5 [ownership receipt](ownership_receipt.json) covers all 33 owners and 59 measured row records. Every row has status `ok`; the full image gate is `MATCH` with zero differing words. The runtime row is explicitly modeled with `t272`, not credited as direct genuine equality. Both collector parent rows (`slus/code` and `slus/code2`) were measured; the two partition-only owners receive data ownership and no placement grant.

The existing `list_cursor_e0` and `runtime_directory` placement certifiers both passed in the private fixture and wrote only private certificates. The [summary receipt](receipt.json) records their hashes, image/graph counts, compiler-model scope, and exact owner-object comparisons. [Input pins](input_pins.json) cover the payload, stage sources/config/headers, all stage objects, guarded assembler, and the two source-overlay receipts. [Production before](production_before.json) and [after](production_after.json) snapshots are byte-identical (SHA-256 `1d07b7d1ce20b769d24ab614ee1a4e25484d1b50158328473b0d5b338f086e9e`); input pins before/after are also byte-identical (SHA-256 `910104feb99211d3ffd66f5619f8757f0e106b59f0e52cb80db16d8ab7466bde`).

An earlier baseline attempt stopped at `gp_d92c_owned: C includes must match declared member order exactly`. The canonical-source overlay moved the body into its declared row and made the aggregator include it once; its complete compiler stream differs only by a `.file` source-name directive. The final full proof then passed without modifying the prover or loosening any check. A separate initial CLI attempt hit genuine ASPSX's long scratch-path limit; the private wrapper is the same short-path setup used by the established live ownership probe.

Reproduction: run [run.py](run.py) with `prepare`, `prove`, and `postflight` in a fresh `full_ownership/` package, using [prover_cli.py](prover_cli.py) as the short-path CLI adapter. The existing fixture and logs are retained under `.ignore` for review. The complete proof grants data ownership only; the two explicit certifier outputs are the only placement recertifications here.
