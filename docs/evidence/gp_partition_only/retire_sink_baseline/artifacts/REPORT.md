# Private `_sink_call_separated_la` retirement probe

The private candidate removes only the `_sink_call_separated_la` method and its single `process_lines` call from the pinned guarded generic package. The CLI remains byte-identical (SHA-256 `d3e6ec7d120abe00f963c82258dfdeb0e228774adce646177525f1dbed925caf`); the retired package is `b89099797be1352d3122455164062d428dcc74529344a919d169e05f663afc52`. The [two-file supplemental patch](supplemental.patch) includes that package deletion and one synthetic test update; it replays exactly against the guarded package and migrated test base. No other assembler code or flags changed.

The [reconciled census](../census_report/summary.json) reports zero firings and zero required uses on all 6,767 logical rows and their physical units. This is a scoped pass-use observation, not a claim that all 6,767 rows have unmasked retail or genuine equality. Historical documentation describes `slus/w_8004AB7C` as an earlier positive control; its final canonical C now compiles without this pass. At the exact final fixture recipe (`2.7.2-cdk`, empty compiler/assembler flags), guarded, retired, and final fixture `w_8004AB7C.o` are **byte-identical** (SHA-256 `ec50b0f0dcc399fb1e586450aa2581dd89f810ceb5c305551474b87b4268ee0c`). That comparison uses the actual `tools/cc.sh` pipeline from a read-only copy of the final GP57 build root and writes objects only here.

The copied 418-test migrated assembler suite is candidate-bound through both its CLI paths and package import. Its original synthetic call-separated-loop test failed exactly because it expected the deleted rewrite. The private test now asserts the entire 18-line retained-seed output, including the ordinary delay-slot and load-hazard nops; all other tests are unchanged from the migrated suite. The final run passes **418/418**. The exact command and log hash, candidate hashes, final source/recipe/object pins, patch replay, and before/after input checks are in [receipt.json](receipt.json). [prepare.py](prepare.py) and [verify.py](verify.py) reproduce the candidate and probe.

The separate [combined GP57 rehearsal](combined_receipt.json) copied the frozen GP57 fixture, rewired every copied build link into the new fixture, rebuilt all 860 physical C objects with the retired package, and proved that every object remained byte-identical. Normal configure regenerated the pinned 860/884 graph, the linked image matched retail, schema-5 ownership passed all 33 modules and 59 measured rows with the explicit `t272` runtime row, and both existing placement certifiers passed. The frozen fixture and production snapshots remained unchanged. This is the pre-validation-refresh baseline: a separate validation-only `aspsx_diff`/trace-doc correction is under review, so the final combined receipt will be rerun in another fresh copy after those tool bytes are frozen.

This package is private preparation. It has not modified production or the frozen GP57 payload and does not by itself authorize pass retirement. The separate full overlay gate is being measured by another agent.
