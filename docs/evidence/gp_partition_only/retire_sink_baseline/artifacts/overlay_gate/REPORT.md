# Sink-retirement candidate: full overlay-window gate

The private candidate that removes `_sink_call_separated_la` passed the forced overlay gate: **2,175/2,175 windows `MATCH`**, with no serial retries. The run used four workers in the isolated `build_ovl_retire_sink_20260924_sol` root. Both `AZURE_MASPSX` and `AZURE_MASPSX_COMPANION` pointed to the candidate CLI; no production assembler or frozen GP57 payload was changed. [receipt.json](receipt.json) and [journal.jsonl](journal.jsonl) record the exact result; [gate.log](gate.log) ends `done 2175 (+0 serial retries) in 975s: {'MATCH': 2175}`.

The candidate CLI SHA-256 was `d3e6ec7d120abe00f963c82258dfdeb0e228774adce646177525f1dbed925caf`; its `maspsx/__init__.py` SHA-256 was `b89099797be1352d3122455164062d428dcc74529344a919d169e05f663afc52`. Both were identical before and after the run. The tracked `src/`, `config/`, `tools/`, `include/`, and `ledger/splits/` snapshot was also identical before and after: `f1dd11c3af72327fadb55359e4f23632584fe2028f9a1bcfb5dc2bf9dc56fb47` over 9,749 tracked paths.

[run.py](run.py) is the one-shot recipe. From the repository root it sets up the unique overlay root with `EXP=retire_sink_20260924_sol SRCROOT=$PWD/src bash tools/build/mk_ovl_root.sh`, then runs `python3 tools/build/gate_all.py --all --workers 4` with `GATE_BUILD_ROOT=build_ovl_retire_sink_20260924_sol`, `GATE_JOURNAL` in this directory, and both assembler variables bound to the candidate CLI. Its setup, import, and gate commands exited zero. It refuses an existing build root or journal to prevent a stale append; a replay needs a new unique experiment name and output directory.

The installed production unit discovery retained one synthetic assertion for the retired pass. Against the guarded baseline, the candidate added **only** `test_call_separated_selfinc_loop_is_sunk_and_materialized` to the existing failure set; those production discovery runs were not clean under either assembler. The audit lane migrated that expectation in a private fixture without changing the assembler further, then passed **418/418** candidate-bound tests with zero failures or errors ([audit receipt](../receipt.json), [unit log](../unit.log)). This gate does not itself authorize production activation or claim a whole-tree toolchain census.
